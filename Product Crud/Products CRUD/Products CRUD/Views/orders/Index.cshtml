@model IEnumerable<Products_CRUD.order>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.customer_id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.orderDetail.id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.orders1.id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.order1.id)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.customer_id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.orderDetail.id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.orders1.id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.order1.id)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
